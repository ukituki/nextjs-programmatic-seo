// jest.setup.js
// In case you need to import jest-dom for custom matchers:
// import '@testing-library/jest-dom';

// Or other global setup

// Mock process.env.NEXT_PUBLIC_ACTIVE_NICHE for tests
process.env.NEXT_PUBLIC_ACTIVE_NICHE = 'restaurants'; // Default to one for consistent testing
